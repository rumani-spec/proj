html bugs 
- Sign-in button hidden off-screen
- .sign-in-btn has top: -9000px, making the button invisible.
- Invisible overlay div above button
- <div class="btn-hitbox"></div> sits on top of the button, blocking clicks.
- Input box wrong type
- <input type="text"> instead of type="number, misleading for 4-digit code entry.
- Progress bar nested incorrectly
- <progress> inside <div> but braces/styles broken in CSS, causing layout issues.
- Script loaded inside nested <div>
- <script src="script.js"></script> placed inside a <div>, unusual and confusing.

js bugs
11. 	Infinite recursion in 
• 	Function calls itself endlessly, freezing the browser.
12. 	Button click triggers infinite loop
• 	 → hangs system.
13. 	Security check rejects all codes except 0777
• 	Logic only accepts , everything else denied.
14. 	Nested shadowing logic
• 	Inner block redefines  variable, confusing debugging.
15. 	Missing  function
• 	Called but never defined, throwing console error.

css bugs 
6. 	Button hidden far off-screen
• 	 pushes it out of view.
7. 	Pseudo-element shield
• 	 creates an invisible overlay with , blocking clicks.
8. 	Progress bar style missing closing brace
• 	Nested  inside  without proper closure breaks cascade.
9. 	Invalid unit
• 	 → invalid unit prevents proper rendering.
10. 	Malformed media query
• 	 missing closing parenthesis.
